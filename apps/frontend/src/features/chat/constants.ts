import type { Message } from "./types";

// Empty array constant to use as stable reference when no messages exist
export const EMPTY_MESSAGES: Message[] = [];

export const MAX_USER_MESSAGE_HEIGHT = 200;

export const MAX_HEIGHT_REM = 12.5;